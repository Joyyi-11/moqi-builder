# {{INSTANCE_NAME}}

这是 {{USER_NAME}} 的 Moqi（默契）实例。

```text
entrypoints/       运行入口
MSA/               Memory / Soul / Agent
references/        按需执行细则
ALIGNMENT.md       对齐控制平面
scripts/           结构校验
```

共享运行规则的唯一真实源是 `entrypoints/AGENTS.md`。个人履历和动态数据使用外部资料源，不复制进 MSA。
